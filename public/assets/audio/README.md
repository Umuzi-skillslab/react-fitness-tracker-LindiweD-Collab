Place motivational audio tracks (.mp3) here, named to match the `audioUrl` path in src/data/exercisesData.js (see motivationTracks). The AudioPlayer component falls back to text if a file is missing.
