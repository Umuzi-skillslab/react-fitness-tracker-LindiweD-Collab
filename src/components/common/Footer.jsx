import styles from './common.module.css';

/** Footer — simple site footer shown on every page. */
const Footer = () => (
  <footer className={styles.footer}>
    <p>Pulse Fitness Tracker · Built for consistent, trackable progress.</p>
  </footer>
);

export default Footer;
